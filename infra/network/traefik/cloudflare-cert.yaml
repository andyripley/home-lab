apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cloudflare-cert
  namespace: traefik
spec:
  secretName: cloudflare-cert
  dnsNames:
    - "ley.rip"
    - "*.ley.rip"
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
