apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: prj-home-lab
      auth:
        workloadIdentity:
          serviceAccountRef:
            name: sa-external-secrets
            namespace: external-secrets
