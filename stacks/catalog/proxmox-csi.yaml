import:
  - catalog/_defaults

components:
  terraform:
    proxmox-csi:
      providers:
        kubernetes:
          # config_path: !store.get home-lab/gcp talos kube_config
      metadata:
        component: proxmox-csi
      vars:
        pve_cluster_name: "pve-cluster"
        pve_endpoint: "https://pve1.ley.rip:8006"
