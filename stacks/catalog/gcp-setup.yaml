terraform:
  backend_type: gcs
  backend:
    gcs:
      bucket: "home-lab-state"
  providers:
    google:
      project: "prj-home-lab"

components:
  terraform:
    gcp-setup:
      metadata:
        component: gcp-setup
      vars:
        bucket_name: "home-lab-state"
        location: "us-east1"
        pve_username: "tofu@pam"
        pve_password: !env PROXMOX_VE_SSH_PASSWORD
